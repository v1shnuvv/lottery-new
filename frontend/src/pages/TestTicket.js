import { useEffect, useState } from "react"

export default function TestTicket(){

    const [samplearray,setSamplearray]=useState([]);
    useEffect(()=>{
        let temp=[...samplearray]
        for(const iterator of temp)
        {
            temp.pus
        }
    },[])

    return<>

    </>
}